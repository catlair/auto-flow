{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "com.catlair.autoflow",
  "description": "Auto Flow 默认能力：前端订阅后端 rpc_event、调用自定义命令与文件对话框。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
