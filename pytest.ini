[pytest]
# 固定临时目录到工作区内。
# 默认的临时根（$TMPDIR/pytest-of-<user>）在受限/沙箱环境下重建时会被拒绝
# （mkdir(exist_ok=True) 命中已存在目录会报 PermissionError），导致第二次跑测试全量报错。
# 放到仓库内可保证重复执行稳定，失败产物也便于直接查看。
addopts = --basetemp=.pytest-tmp
